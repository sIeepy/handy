%table.table.table-striped
  %thead
    %tr
      %th
        Announcement
      %th
        Created
  %tbody
    - announcements.each do |annon|
      %tr
        %td
          = link_to "#{annon.title}", announcement_path(annon.id)
        %td
          = annon.decorate.creation
